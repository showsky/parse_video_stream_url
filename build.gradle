apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: '**/*.jar')
}

sourceSets {
    main {
        java {
            srcDir 'src/java'
        }
        resources {
            srcDir 'src/resources'
        }
    }
}